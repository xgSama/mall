spring:
  application:
    name: mall-product
  cloud:
    nacos:
      config:
        server-addr: nacos:8848
        namespace: xgsama-dev

feign:
  httpclient:
    connection-timeout: 6000000


ribbon:
  # 请求处理的超时时间
  ReadTimeout: 600000
  # 请求连接的超时时间
  ConnectTimeout: 30000