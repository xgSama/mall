<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--suppress SqlNoDataSourceInspection -->
<mapper namespace="com.xgsama.mall.product.dao.SkuSaleAttrValueDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.xgsama.mall.product.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>

    <resultMap id="SkuItemSaleAttrVo" type="com.xgsama.mall.product.vo.ItemSaleAttrVo">
        <result column="attr_id" property="attrId"/>
        <result column="attr_name" property="attrName"/>
        <collection property="attrValues" ofType="com.xgsama.mall.product.vo.AttrValueWithSkuIdVo">
            <result column="attr_value" property="attrValue"/>
            <result column="sku_ids" property="skuIds"/>
        </collection>
    </resultMap>

    <!--suppress SqlResolve -->
    <select id="getSaleAttrsBuSpuId" resultMap="SkuItemSaleAttrVo">
        SELECT ssav.`attr_id`, ssav.`attr_name`, ssav.`attr_value`, GROUP_CONCAT(DISTINCT info.`sku_id`) sku_ids
        FROM `pms_sku_info` INFO
                 LEFT JOIN `pms_sku_sale_attr_value` ssav ON ssav.`sku_id` = info.`sku_id`
        WHERE info.`spu_id` = #{spuId}
        GROUP BY ssav.`attr_id`, ssav.`attr_name`, ssav.`attr_value`
    </select>

    <!--suppress SqlResolve -->
    <select id="getSkuSaleAttrValueAsStringList" resultType="java.lang.String">
        select concat(attr_name, ": ", attr_value)
        from pms_sku_sale_attr_value
        where sku_id = #{skuId}
    </select>


</mapper>